import test, { describe, mock } from "node:test";

test("test.service", { }, (t) => {

    t.test("it should work", (t) => {
        t.test("What the frick")
    })
    const prismaMock = mock.fn()
})